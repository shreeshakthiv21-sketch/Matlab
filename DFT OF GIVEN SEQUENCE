clc
close
clear

x= input("Enter sequence: ");
N= length(x);

for k= 0:N-1
    X(k+1)=0;

    for n= 0:N-1
        X(k+1)= X(k+1)+ x(n+1)*exp((-1*j*2*pi*n*k)/N);
    end
end
disp("DFT OF YOUR SEQUENCE:")
disp(X)
